"use client";
import { motion } from 'framer-motion';

interface TestimonialProps {
  name: string;
  role: string;
  text: string;
  emoji: string;
  delay: number;
}

const TestimonialCard = ({ name, role, text, emoji, delay }: TestimonialProps) => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 30 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 0.6, delay }}
      whileHover={{ y: -10 }}
      className="bg-white p-8 rounded-2xl shadow-lg transition-all duration-300"
    >
      <div className="w-24 h-24 rounded-full bg-azul-celeste mx-auto mb-4 
                    flex items-center justify-center text-4xl text-white">
        {emoji}
      </div>
      <h3 className="text-xl font-semibold text-azul-petroleo mb-2">{name}</h3>
      <p className="text-azul-celeste text-sm mb-4">{role}</p>
      <p className="text-gray-600 italic">{text}</p>
    </motion.div>
  );
};

const Testimonials = () => {
  const testimonials = [
    {
      name: "Dr. Carlos Mendoza",
      role: "Médico Cirujano | Canadá",
      text: "EPSeak me ayudó a aprobar mi examen médico internacional. Ahora trabajo en Toronto cumpliendo mi sueño.",
      emoji: "👨‍⚕️",
      delay: 0.2
    },
    {
      name: "María González",
      role: "Contadora | Estados Unidos",
      text: "La metodología específica para mi profesión hizo toda la diferencia. En 6 meses logré mi objetivo.",
      emoji: "👩‍💼",
      delay: 0.4
    },
    {
      name: "Luis Ramírez",
      role: "Ingeniero de Software | Alemania",
      text: "El inglés técnico que aprendí me abrió las puertas en empresas europeas. Totalmente recomendado.",
      emoji: "👨‍💻",
      delay: 0.6
    }
  ];

  return (
    <section id="experiencia" className="bg-gris-suave py-20">
      <div className="container mx-auto px-4">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="section-title"
        >
          Experiencia con nosotros
        </motion.h2>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {testimonials.map((testimonial, index) => (
            <TestimonialCard
              key={testimonial.name}
              {...testimonial}
            />
          ))}
        </div>
      </div>
    </section>
  );
};

export default Testimonials;